---
id: canonicalize-tool-surface-json-to
level: task
title: "Canonicalize tool_surface.json to package copy + honesty edits (scope-policy claim, child/parent removal)"
short_code: "RKIT-T-0079"
created_at: 2026-08-16T18:48:24.127521+00:00
updated_at: 2026-08-16T18:48:24.127521+00:00
parent: make-career-mcp-importable-and
blocked_by: []
archived: false

tags:
  - "#task"
  - "#phase/todo"


exit_criteria_met: false
strategy_id: RKIT-S-0001
initiative_id: RKIT-I-0009
---

# Canonicalize tool_surface.json to package copy + honesty edits

## Parent Initiative

[[RKIT-I-0009]]

## Objective **[REQUIRED]**

Make `career-mcp/career_mcp/tool_surface.json` the single canonical career-mcp manifest (RKIT-A-0002 item 4) and apply the two authorized honesty edits: remove the false `tools/call` "enforces scope policy" claim (`career_mcp/__init__.py:56-57` merely echoes context — RKIT-A-0002 item 2) and remove the `child`/`parent` relationship-type advertisements the store rejects at runtime (career-store `store_surface.json` relationship_types = alias/equivalent/related/contradicts). Because `tools/career_mcp_guardrails.py` is PROTECTED and validates the ROOT copy, the root copy is retained this task as a byte-identical generated artifact of the package copy; the guardrail re-path (and root-copy deletion) is deferred to Daniel's approval batch (RKIT-T-0080 authors the patch).

## Acceptance Criteria **[REQUIRED]**

- [ ] Package copy `career-mcp/career_mcp/tool_surface.json` is the edit target: scope-policy claim removed from the tools/call description; `child`/`parent` removed from the relationship-type vocabulary block AND the `career.add_relationship` schema enum (an advertised value must not fail inside the store — the worst failure shape for agent callers).
- [ ] Root `tool_surface.json` (unprotected) is regenerated byte-identical to the package copy, with a generation marker comment/field if the format allows one without breaking the guardrail's validation; if not, plain byte-copy.
- [ ] A small unprotected sync tool (e.g. `career-mcp/tools/sync_tool_surface.py` or equivalent existing-idiom location) regenerates root from package; running it twice is a no-op.
- [ ] Runtime still loads the package copy (`career_mcp/__init__.py:199-204` unchanged behavior); existing 19 career-mcp contract tests stay green (update any that asserted the removed claims — weakening-by-honesty is authorized by RKIT-A-0006, but keep assertion strength otherwise).
- [ ] `python3 tools/career_mcp_guardrails.py` (validating the root copy) still passes — the copies are identical so the honest manifest satisfies it; if the protected guardrail asserts the removed content verbatim, STOP, defer that edit, and report (do not end red).
- [ ] `--pr` and `--smoke` green; `straight-jacket verify` stays clean.

## Implementation Notes **[CONDITIONAL: Technical Task]**

### Technical Approach
- Check first how tests and guardrails reference the removed strings (`grep -rn "scope policy\|child\|parent" tools/career_mcp_guardrails.py tests/`), and read RKIT-A-0002 for the decided wording direction.
- Runtime rejection of child/parent already happens in the store; after this edit it fails at MCP schema validation instead — assert that improved failure shape in tests (T-0080 covers the systematic version).
- Recommended Agent: opus + medium

### Dependencies
None. RKIT-T-0080 builds on the canonical copy.

### Risk Considerations
- PROTECTED (read-only): tools/career_mcp_guardrails.py, tests/boundary/test_career_mcp_guardrails.py, all tools/* protected files. Any needed guardrail change is DEFERRED with a verbatim patch in the report.
- Do not touch career-store or workflow packages.

## Status Updates **[REQUIRED]**

*To be added during implementation*

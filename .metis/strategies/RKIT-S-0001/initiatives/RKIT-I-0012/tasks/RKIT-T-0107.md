---
id: truthful-manifest-policy-statement
level: task
title: "Truthful manifest policy statement, parity test, TEST_SPEC strengthening"
short_code: "RKIT-T-0107"
created_at: 2026-08-17T19:29:02.815521+00:00
updated_at: 2026-08-17T19:38:29.419657+00:00
parent: add-career-mcp-policy-scope-and
blocked_by: [RKIT-T-0106]
archived: false

tags:
  - "#task"
  - "#phase/active"


exit_criteria_met: false
strategy_id: RKIT-S-0001
initiative_id: RKIT-I-0012
---

# Truthful manifest policy statement, parity test, TEST_SPEC strengthening

## Parent Initiative

[[RKIT-I-0012]]

## Objective

Close the audit's tool_surface.json:21 contract-misalignment finding permanently (R5/R6): the canonical manifest gains a policy block stating EXACTLY the v1 posture (single-user local; mutations gated on host-mediated confirmation; NO scope enforcement), a parity test asserts statement↔behavior agreement so the manifest can never overstate again, and career-mcp/TEST_SPEC.md is strengthened so any future manifest policy claim requires corresponding executable tests.

## Acceptance Criteria

## Acceptance Criteria

- [ ] Canonical manifest `career-mcp/career_mcp/tool_surface.json` gains a `policy` block: single-user local posture (RKIT-A-0002 item 2), mutations gated on host-mediated `confirmed` argument, explicit "no scope enforcement in v1" statement. NO claim beyond what T-0106's code enforces. Generated byte-copy regenerated via `career-mcp/tools/sync_tool_surface.py`; byte-identity contract test stays green.
- [ ] Parity test (extend `tests/contract/test_career_mcp_manifest_parity.py` or sibling): asserts (a) the policy block's declared gated-tool set equals the policy module's mutating classification exactly; (b) the block claims no scope enforcement AND no scope/principal/role vocabulary appears anywhere in the manifest; (c) behavioral spot-check — a declared-gated tool actually rejects unconfirmed mutations (reuse T-0106's test helpers). Statement drifts from behavior ⇒ named test fails.
- [ ] career-mcp/TEST_SPEC.md strengthening (the audit's root-cause fix): replace the weak ":35 authorization/scope policy if added later" optionality with a binding rule — any policy claim in the manifest MUST have named executable tests — plus explicit confirmation-enforcement items (gated unconfirmed mutation, satisfied confirmation, computed flags) each naming its covering test from T-0106/T-0107.
- [ ] Single-user posture recorded (R6): grep-proof that no scope enforcement is advertised anywhere in v1 manifests/docs for career-mcp.
- [ ] Gates green: `--pr`, `--smoke`, `--future-contract`. No protected edits (career_mcp_guardrails re-paths to the canonical manifest — confirm the policy block doesn't trip its schema pinning; defer, don't end red, if it does).

## Implementation Notes

### Technical Approach
Small, mostly declarative task; the substance is the parity test's structural assertions. Mutation-probe: hand-weaken the policy block claim (e.g. add a fake "enforces scopes" sentence or drop a tool from the gated set) and confirm the parity test fails by name.

### Dependencies
RKIT-T-0106 (the behavior the statement must match).

### Risk Considerations
Guardrail manifest pinning (protected) — read career_mcp_guardrails.py expectations before adding the block.

Recommended Agent: opus + medium

## Status Updates

- 2026-08-17: Hardened manifest policy parity by adding explicit `policy.gated_tools`, full manifest vocabulary guard with the `role` fact-type exception, and per-declared-gated-tool unconfirmed mutation rejection coverage using `FakeCareerStore`. Tightened `career-mcp/TEST_SPEC.md` so manifest policy claims require named executable tests. Regenerated `career-mcp/tool_surface.json` from the canonical manifest. Mutation probes confirmed gated-set drift and forbidden scope wording fail the named parity tests. Gates green: `--pr`, `--smoke`, `--future-contract`. Straight Jacket still reports the pre-existing expected `tools/resume_agent_guardrails.py` mismatch only.

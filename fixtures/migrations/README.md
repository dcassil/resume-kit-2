# Migration Fixtures

This folder is reserved for versioned career-store migration fixtures. The fixture contract requires
the directory to exist before migration tests are implemented so test paths remain stable.

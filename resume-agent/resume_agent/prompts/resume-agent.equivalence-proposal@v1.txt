You propose semantic equivalence candidates for resume tailoring.

Input JSON contains code-selected candidate pairs and evidence snippets. Do not
search beyond those candidates. For each supported pair, return one JSON array
item with exactly these fields:

- id
- term_a
- term_b
- direction
- rationale
- evidence_refs
- confidence
- requires_validation

Direction must be one of equivalent, narrower_than, or broader_than. Use
narrower_than when term_a is a narrower claim that can support the broader
term_b. Use broader_than when term_a is broader than term_b. Every evidence_ref
must be one of the supplied evidence ids. Set requires_validation to true for
every item because downstream code validates proposals before use.

Return only JSON matching schema resume-agent.equivalence-proposal.v1.
